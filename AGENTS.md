# AGENTS.md

Compact guidance for OpenCode sessions working in this repo. Verify against the executable config if anything seems stale.

> **铁律优先**：本人的硬性要求见 [`RULES.md`](./RULES.md)（需求先问清、代码高内聚低耦合、日志详实、前端统一+移动端适配、不做无关优化、危险操作必确认、中文提交、文档与代码一致、提交前查隐私泄露）。该文件优先级高于本文件，冲突以 `RULES.md` 为准。

## 项目定位

Stellar 是**个人知识/实验沉淀池**，不是纯后台管理工具。游客可免登录浏览落地页与关于我、体验所有工具（受 IP 单日限流保护）；管理功能登录后可见。公开页与管理后台**共用 `BasicLayout`**，按登录态切换可见菜单与入口。

- **落地页 `/home`**：精简门户，hero 简介 + 公开工具卡片导航 + "关于我"入口；`/` 默认指向此处，登录用户也可见。
- **关于我 `/about`**：简历式个人主页（公开，菜单置末），Hero（头像/昵称/头衔/简介/社交链接）+ 富文本"关于我" + 技能标签 + 项目展示卡片网格 + 联系方式；后台 `system/profile` 编辑。
- **公开菜单可配置**：哪些路由对游客可见由管理后台 `sys_menu_visibility` 决定，前端拉 `/public/menu-config` 过滤，非写死。
- **公开接口放行**：自定义 `@PublicAccess` 注解标注游客可调方法；Sa-Token 拦截器识别注解跳过 `checkLogin`（默认仍全拦截，仅标注方法放行），方法内用 `StpUtil.isLogin()` 区分游客/登录。
- **IP 限流**：基于 Redis INCR + 当日过期，按 IP 单日计数，超限返回 `Result` 429；阈值配置化。多实例共享计数。
- **计费/统计**：AI token 消费记录（`sys_ai_usage`，主体 account/ip，含 provider_id/model_type 维度）+ IP 日调用次数限制（`@RateLimit`）。`AiChatService` 请求 LLM `stream_options.include_usage` 获取精确 token，不返回则字符估算兜底；首页仪表盘展示总/今日 token、调用次数、近 7 日趋势。AI 文案对游客开放（IP 日限 5 次）。AI 支持多供应商多模型（`sys_ai_provider`+`sys_ai_model`，模型按类型标注 TEXT/IMAGE/AUDIO/EMBEDDING/VIDEO，按类型设默认），图片生成对游客开放（IP 日限 2 次，产物存 `sys_file` 永久化）。
- **游戏模块**：一级菜单 `Game`（`/game`）下挂数学游戏等子菜单，对游客公开（`requiresAuth: false` 天然公开）。数学游戏 `/game/math` 完整还原"十以内加减法记忆游戏"：30 题、每 3 秒闪现、第 6 题起滞后 5 题凭记忆作答、计分/正确率/用时/逐题复盘；成绩存 `sys_game_score` 表，`GameController` `@PublicAccess` 开放提交（`@RateLimit(daily=30)` 防刷）与排行榜查询（前 100）。
- **神奇海螺**：Game 下子菜单 `/game/conch`（天然公开），海绵宝宝占卜道具玩法——用户提问，AI 从管理员预设的回答库（文本+音频）里语义匹配 top-3 随机选 1 条回答，自动播放音频并展示文本；失败兜底全库随机。预设存 `conch_answer` 表（answer_text/match_description/file_id 引用 sys_file），提问历史存 `conch_record` 表（不记 IP）。`ConchController`（`/game/conch/**`）`@PublicAccess + @RateLimit(daily=10)` 开放提问与音频读取；`AiChatService.chatCompletion` 非流式调 LLM（TEXT 默认模型）拿完整结果；AI 匹配开关存 `sys_setting`（key=`conch_ai_enabled`）；后台 `game/conch-admin` 管理预设与查看提问历史。

## 规划中的改造（Roadmap）

> 随实现推进，对应行为规范同步补写进本文档，避免文档超前于代码（铁律8）。

- **阶段 0** 鉴权下沉 + 公开壳骨架：前端 `requiresAuth` 下沉到子路由、新增 `/home`、`useMenu` 按登录态过滤、`BasicLayout` 游客态隐藏多标签页；后端 `@PublicAccess` 注解 + `SaTokenConfig` 改造。
- **阶段 1** 可配置公开菜单：`sys_menu_visibility` 表 + `GET /public/menu-config` + 后台可见性配置页（`system/menu-visibility`）。
- **阶段 2** 首页落地页 + 关于我（按 **2b→2a** 推进）：
  - 2b 文件上传：`FileController` 上传图片存数据库 `sys_file` 表（二进制 BYTEA），登录方可上传、`GET /file/{id}` 游客可读（`@PublicAccess`）。
  - 2a `sys_profile` 扩展 `title/about/location` 字段，新增简历式 `/about` 页（Hero + 富文本"关于我" + 技能 + 联系方式）；`/home` 精简门户 + "关于我"入口。后台 `system/profile` 编辑。
- **阶段 3** IP 单日限流：`@PublicAccess` 方法按 IP 计数，超限 `Result` 429，阈值配置化。
- **阶段 4（已实现）** AI token 统计 + IP 日限：`sys_ai_usage` + `AiChatService` usage 解析/估算兜底 + `AiChatController` `@PublicAccess+@RateLimit(daily=5)` 对游客开放 + `dashboard` 统计看板（总/今日 token、调用次数、近 7 日趋势）。
- **阶段 5（已实现）** 游戏模块：`Game` 一级菜单 + 数学游戏子菜单（`/game/math`，天然公开），记忆型十以内加减法游戏；`sys_game_score` 表 + `GameController`（`@PublicAccess` 提交 `@RateLimit(daily=30)` / 排行榜查询）。
- **阶段 6（已实现）** 神奇海螺：Game 下子菜单 `/game/conch`（天然公开），海绵宝宝占卜道具。`conch_answer` 表（answer_text/match_description/file_id 引用 sys_file/enabled）+ `conch_record` 表（question_text/answer_id/user_id，不记 IP）。`ConchController` `@PublicAccess + @RateLimit(daily=10)` 开放提问（`POST /tts/conch/ask`）与音频读取（`GET /tts/conch/answer/{id}/audio`）；`ConchService.ask` 取启用预设 → `AiChatService.chatCompletion`（新增非流式方法）让 LLM 返回 top-3 id → 随机选 1，失败/格式错全库随机兜底；预设管理 `system/conch`（增删改查 + `NUpload` 上传音频，复用扩展后的 `FileController` 音频白名单）。
- **阶段 7（已实现）** 多供应商多模型 + 图片生成：`sys_ai_provider`（endpoint+apiKey+available_models 缓存）+ `sys_ai_model`（provider_id/model/model_type/is_default/enabled，按类型设默认互斥）两表替代旧单条 `sys_ai_config`（启动时幂等迁移旧配置→1 供应商+1 TEXT 模型）。模型类型用字典表 `sys_dict_type`+`sys_dict_data` 管理（首批种 model_type：TEXT/IMAGE/AUDIO/EMBEDDING/VIDEO，后三种预留）。全局开关迁 `sys_setting`（key-value，`conch_ai_enabled` 迁入，`SysSettingController` GET/PUT `/setting/{key}`）。`AiModelService.resolveConfig(modelId)`/`resolveDefaultConfig(type)` 返回 `AiResolvedConfig`（endpoint/apiKey/model/modelType/providerId），`AiChatService` 按 modelId 解析配置（自带 key>modelId>TEXT 默认），`sys_ai_usage` 加 provider_id+model_type。新增 `AiImageService`（`/v1/images/generations`，b64_json→存 `sys_file`）+ `POST /ai/image/create` 异步任务（`@PublicAccess+@RateLimit(daily=2)` 游客开放，立即返回 taskId，`@Async` 生成+存库，前端轮询 `GET /ai/image/task/{id}`）。前端 `system/ai-config` 改两级页面（供应商列表+模型列表），文案工具加 TEXT 模型下拉（`streamAiChat` 支持 modelId），新增 `/video/image` 图片生成页（天然公开）。
- **阶段 8（已实现）** AI 生成历史日志 + 三页统一工作台布局：文案/图片/视频三页点击生成后右下常驻 `NDataTable` 历史（请求时间/提示词/结果/返回时间/耗时/操作），像日志列表一样查看历史。新增 `sys_ai_chat_record` 表（subject_type=account/ip、provider_id、model、prompt、result、status、error_msg、request_time、response_time、duration_ms），`AiChatService.doStreamChat` 流式结束自动落库（带幂等守卫，异常吞掉不影响流式），`AiChatRecordController`（`/ai/chat/record`，`@PublicAccess` 分页/删除/清空，登录按账号、游客按 IP）；文案工具不再前端显式调保存、不再依赖 `sys_ai_copy_result`（该表与 `AiCopyResultController`/`Service`/实体/Mapper/DTO 已删除，DB 表数据保留）。图片历史复用 `sys_ai_image_task`（`create_time`=请求时间、`update_time`=返回时间），`AiImageService.pageHistory` + `GET /ai/image/page`（`@PublicAccess`，按主体分页），`AiImageTaskVO` 补 `updateTime/durationMs/size/ratio`。视频历史新增 `sys_ai_video_task` 表（本地留痕：`AiVideoService.createTask` 落库、`getTask` 轮询更新本地行 status/file_id、completed 复用本地 file_id 避免重复下载），`GET /ai/video/page`（需登录，按账号分页），`AiVideoCreateDTO` 补 `ratio/duration`（历史展示用）。三页统一抽 `views/video/components/AiGeneratorLayout.vue` 公共骨架（左 360px 配置栏 sticky + 右历史单卡 + 内置 `NDrawer` 壳，`<1024px` 栈叠单列、抽屉全屏），三页填 `aside/history/drawer` slots，替换原 `max-width:720px` 单列居中布局以利用横向空间。历史点"查看"开右侧抽屉回看，去掉 result 卡、生成中按钮 loading + `message` 提示，完成后自动弹抽屉展示最新一条，结果全走历史/抽屉，避免历史结果撑高把历史卡顶出视口；文案详情复用 `CopyResultDetail.vue` 子组件。游客历史按 IP 可见（同 IP 共享，已知隐私折衷）。
- **阶段 9（已实现）** AI 任务通知 SSE 长连接替代轮询：图片/视频生成不再前端定时轮询，改用 SSE 全局长连接 + Redis pub/sub 多实例广播。新增 `SseEmitterManager`（按 subject=`account:{userId}`/`ip:{ip}` 分组管理 emitter，`@Scheduled` 30s 心跳保活）、`AiNotifyController`（`GET /ai/notify` `@PublicAccess` 返回 `SseEmitter`）、`AiNotifyPublisher`/`AiNotifyListener`（Redis pub/sub channel=`stellar:ai:notify`，任务完成时 `convertAndSend` 广播，各实例订阅后推本地 SSE 连接）、`AiNotifyMessage` record（subject/type=image·video/taskId/status=completed·failed）。图片 `AiImageTaskWorker.doGenerateAsync` 完成/失败时发通知；视频新增 `AiVideoTaskWorker`（`@Async("aiTaskExecutor")` + `@EventListener` 监听 `VideoTaskCreatedEvent`，循环轮询供应商 5s 间隔/10min 超时，复用 `AiVideoService.getTask` 查+下载+更新，完成/失败/超时发通知），`AiVideoService.createTask` 落库后发事件触发 worker。前端 `store/aiNotify.ts`（`fetch`+`ReadableStream` 连 `/ai/notify`，带 Authorization header，断线指数退避重连，`onTaskNotify` 注册回调），`BasicLayout` `onMounted` 建立全局长连接、登录态变化重连、`onBeforeUnmount` 断开；图片/视频页删轮询三件套，`onTaskNotify` 收到通知后 `refreshHistory` + 自动弹抽屉，进入页面 `loadHistory` 补全断线期间完成的状态。`AsyncConfig` 加 `@EnableScheduling` 驱动心跳。

- **阶段 10（已实现）** AI 聊天模块：新增一级菜单 `AI聊天`（`/chat`，icon=`chatbubbles`，order=3）下挂 5 子菜单。六张表：`ai_persona`（人设：name/system_prompt/description/enabled/sort_order/built_in，schema 幂等播种 3 个内置——通用助手/程序员/写作助手，`AiBuiltinPersonas` 供恢复默认）+ `ai_chat_session`（会话：title/persona_id/kb_id/subject_type/subject_id，主体 account/ip）+ `ai_chat_message`（消息：session_id/role(system·user·assistant)/content/tokens）+ `ai_memory`（长期记忆：user_id/content/source_session_id，按账号）+ `ai_knowledge_base`（知识库：name/description/embedding_model_id/chunk_count）+ `ai_knowledge_chunk`（分块：kb_id/chunk_text/chunk_index/token_count/source_name/embedding，embedding 列存 JSON 数组文本 `[v1,v2,...]`，纯 Java 内存余弦检索，无 pgvector 依赖）。`AiEmbeddingService` 调 `/v1/embeddings`（按 EMBEDDING 默认模型或 modelId，批量，`toVectorLiteral` 转 JSON 数组文本）。`AiKnowledgeService`（KB CRUD + 文档分块500字+50overlap + 批量向量化每批32 + 纯 Java 内存余弦检索 top-k：加载 KB 全量向量算余弦相似度，无向量/查询向量化失败返回空降级）+ `AiKnowledgeController`（文档纯文本/上传 txt·md，分块查看/删除，重建索引，RAG 检索仅供后端内部调用不对外）。`AiChatService` 扩展多轮：`streamMultiChat(messages, modelId, onComplete)` 流式（不落 sys_ai_chat_record，仅记 token usage，onComplete 回调供会话服务落 assistant 消息）+ `chatCompletionWithMessages(messages, modelId)` 非流式（记忆摘要用，不限制 max_tokens）。`AiChatSessionService`（会话+消息 CRUD 游客按IP归属校验 + 多轮流式：组装 system[人设 prompt + 登录用户长期记忆 + 关联知识库 RAG top4 片段] + 历史最近20条消息，流式完成落 assistant 消息+更新会话 title 首轮截断）+ `AiChatSessionController`（`@PublicAccess` 会话/消息/流式对游客开放，`@RateLimit(daily=20)` 流式 IP 日限，管理员 `messages/admin`+`session/{id}/admin` 删任意会话需登录无归属校验）。`AiMemoryService`（`@Scheduled` cron 每日3点整理近7天未整理的 account 会话为记忆，LLM 提取事实按行落 ai_memory，定时幂等跳过已整理会话，手动 `summarize/{sessionId}` 触发不检查可重复）+ `AiMemoryController`（分页/编辑/删除/手动整理）。前端 `api/chat.ts` 封装全部接口（`streamChat` fetch+ReadableStream 复用 SSE 模式）；5 页：`/chat/talk`（公开 `requiresAuth:false`，ChatGPT 式：左会话列表+右消息流+顶人设/知识库/模型下拉(登录)+底输入 Enter发送Shift换行+停止生成，纯文本 pre-wrap 渲染，流式失败渲染红色错误气泡展示后端 `error` 事件原因而非空白块，游客限流提示，移动端<768 栈叠会话面板max160px）、`/chat/history`（登录，管理员分页所有会话+消息抽屉+删除）、`/chat/memory`（登录，记忆分页+编辑/删除+手动整理会话）、`/chat/knowledge`（登录，KB 列表+分块管理+添加文档/上传 txt·md/重建索引，无 EMBEDDING 模型时警告）、`/chat/persona`（登录，人设 CRUD+启停 NSwitch+内置恢复默认）。聊天对游客开放（IP 日限20），管理页仅登录；游客会话按 IP、kbId 强制 null（无 RAG）、无长期记忆。
- **阶段 11（已实现）** AI 聊天工具调用（function calling：画图/TTS）：登录用户聊天时 LLM 可主动调用画图/TTS 工具，工具产物（图片/音频）挂在 assistant 消息上返回前端渲染。`ai_chat_message` 加 `attachment_type`(image·audio)+`attachment_file_id`(引用 sys_file) 两列；`sys_setting` 种子 `chat_tts_engine`=ai（聊天 TTS 引擎开关：ai=优先AI失败降级Edge / edge=直接Edge；用户在聊天页选了具体音色则按音色所属引擎走覆盖此开关）。新增 `AiChatToolService`（`getToolDefinitions` 返回 OpenAI 兼容 tools schema——`generate_image`(prompt 参数，IMAGE 默认模型未配置时不暴露)+`synthesize_speech`(text 参数，恒暴露 Edge 兜底)；`execute(toolCall, voice)` 按 name 分发执行，工具结果 content 为 JSON 串 `{"status":"success","type":"image·audio","fileId":N,"url":"/file/N"}` / `{"status":"failed","error":"..."}` 回传 LLM；TTS 引擎判定：用户选了音色按格式判断——Edge 音色匹配 `^[a-z]{2}-[A-Z][a-z]-\w+Neural$` 走 Edge，否则走 AI；用户未选按 `chat_tts_engine` 开关；AI TTS 失败自动降级 Edge 用 `zh-CN-XiaoxiaoNeural`+`log.warn`）。`AiImageService.generateImageSync(prompt)` 同步生成（调 `AiImageTaskWorker.generateImageBytes` 提为 public+存 sys_file PNG+写 sys_ai_image_task completed+记 token，**不发 SSE**避免图片页重复弹抽屉）；TTS 工具执行存 sys_file（AI=wav/Edge=mp3）+写 tts_record（让 TTS 历史页可见，operator 取当前登录用户）。`AiChatService` 新增 `chatCompletionWithTools(messages, tools, modelId)` 非流式带 tools（body 加 tools+tool_choice=auto，返回完整 JsonNode 供解析 tool_calls）+ `streamMultiChatWithTools(messages, modelId, tools, voice, Consumer<AiChatResult> onComplete)` 编排：SseEmitter 超时 5min（图片生成可能 30s+流式 60s）→ 第一次非流式带 tools 判定 → 无 tool_calls 直接一次性流式转发 content → 有 tool_calls 取首个执行（提示词约束单工具，违规只执行首个+log.warn）→ messages 追加 [assistant(tool_call), tool(result)] → 发进度事件 `{status:"generating_image"/"generating_audio"}` → 第二次流式不带 tools 避免无限循环 → 转发分片+done → `onComplete(AiChatResult(最终文本, attachmentType, attachmentFileId))`。`AiChatSessionService.streamChat` 改造：签名加 `voice`，登录走 `streamMultiChatWithTools`（落 assistant 消息含 attachment），游客走原 `streamMultiChat`（纯文本无 tools）；首轮标题更新抽 `updateSessionTitle`。`AiChatStreamDTO` 加 `voice` 字段。仅登录用户暴露 tools（游客纯文本聊天），工具调用不单独限流（登录跳过限流）。前端 `AiChatMessage` 类型加 `attachmentType/attachmentFileId/attachmentUrl`（后端实体 `getAttachmentUrl()` 计算返回 `/file/{id}`）；`api/chat.ts` `streamChat` 加 `voice`+`onStatus` 回调（解析 `{status}` 事件）；`constants/tts-voices.ts` 新增 `chatVoiceOptions = ttsVoiceOptions.concat(mimoVoiceOptions)`（合并 Edge+MiMo 分组）；`views/chat/talk` header 加"TTS音色"下拉（仅登录，clearable，placeholder="按系统开关"）+ bubble 渲染（`<img class="msg-image">`/`<audio controls class="msg-audio">`+`<span class="msg-text">` 纯文本）+ streaming typing 进度文字（`generating_image`→"正在生成图片…"、`generating_audio`→"正在合成语音…"、否则"正在思考…"）；`views/chat/history` 消息抽屉复用同款渲染。历史会话重开能再看到图片/音频。

## Repo shape

Two independent packages, **no shared workspace tooling**:
- `frontend/` — Vue3 + Vite + TypeScript + Pinia + UnoCSS + Naive UI. Package manager is **pnpm** (not npm/yarn).
- `backend/` — Spring Boot 3.3 + Java 21 + Maven + MyBatis-Plus + PostgreSQL + Sa-Token. Java package root `com.stellar`, mappers scanned from `com.stellar.{ai,tts,game,system}.mapper`.

**后端按领域分子包**：`com.stellar.ai.*`（controller/service/entity/mapper/dto/vo/event）、`com.stellar.tts.*`、`com.stellar.game.*`、`com.stellar.system.*`、`com.stellar.infra`（SseEmitterManager/ExternalCallLogger/RateLimitService/SubjectUtils）。公共基础（common/config/interceptor/aspect/annotation/enums）保持根级。

**前端导航结构**（阶段12重构后）：
- `AI创作 /ai`（order 3）：创作组（聊天/文案/图片/视频/语音合成）+ 管理组（模板/会话/记忆/知识库/人设）
- `工具 /tools`（order 5）：封面工具
- `游戏 /game`（order 6）：数学游戏/神奇海螺/海螺预设
- `系统管理 /system`（order 99）：用户资料/日志/AI配置/游客访问/个人资料/文件管理

Run them as two separate processes; there is no root-level build that covers both.

## Commands

Frontend (`frontend/`):
```
pnpm dev          # dev server on :5173
pnpm typecheck    # vue-tsc -b --noEmit
pnpm build        # vue-tsc -b && vite build (typecheck gates build)
```

Backend (`backend/`):
```
mvn -q compile -DskipTests          # fast compile check
mvn test -Dtest='SafeUrlValidatorTest,SubjectUtilsTest' -DfailIfNoTests=false   # 跑指定单测
mvn test                             # 跑全部测试（含 src/test 下的纯逻辑单测）
mvn test jacoco:report               # 跑测试并生成覆盖率报告 target/site/jacoco/index.html
$env:SPRING_PROFILES_ACTIVE="local"; mvn spring-boot:run   # run on :8080
```
> 当前覆盖率（2026-08-02，68 个测试类、513 个用例）：整体 **行 69.5% / 分支 58.3% / 指令 68.1% / 方法 78.4%**。**Controller 层已全部补测**（纯 Mockito 单测）：`ai.controller` 98.9% / `game.controller` 100% / `system.controller` 100% / `tts.controller` 96.1%，全部从 0% 拉起；含登录/游客主体解析（`StpUtil` 静态 mock）、IP 代理头穿透、ResponseEntity 响应头断言（Content-Type/Content-Disposition/Cache-Control）、公开接口限流与白名单分支（`AiKnowledgeController` 扩展名校验、`AiMemoryController` userId 类型校验）。`system.service` 已批量补测 23.0%→**97.8%**（AuthService/UserService/ProfileService/ProfileProjectService/MenuVisibilityService/SysSettingService/SysLogService/DashboardService 全测；`DashboardService` 测试需先 `TableInfoHelper.initTableInfo` 注册实体元数据——纯 Mockito 下 MP 的 `LambdaQueryWrapper.select()` 构建时解析列依赖该缓存）；异步 Worker/Listener 已补测（`AiImageTaskWorker` 80.6% 含 b64/URL/错误状态/空数据/超限分支、`AiVideoTaskWorker` 63.5% 含任务不存在/completed/failed/轮询异常续轮、`AiNotifyPublisher`/`AiNotifyListener`/`SseEmitterManager` 100%，`AiVideoTaskWorkerTest` 注意轮询 5s 间隔每个用例一次查询即返回）。剩余缺口：`AiChatService` 异步 SSE 编排（该类行 17.2%，刻意，非确定性）、`TtsService`（5.7%，Edge TTS WebSocket）、`RequestLogInterceptor`（已补 100%）与 config/aspect 低价值包（`DataInitializer`/`RedisConfig`/`LogAspect` 等 Spring 装配类 0%，纯 mock 意义小）。`pom.xml` 已挂 `jacoco-maven-plugin:0.8.12`（仅 test 阶段插桩，不影响生产代码）。已覆盖（AI 核心 service 为主）：`AiTaskService` 100% / `AiPersonaService` 98.6% / `AiTemplateService` 97.9% / `AiMemoryService` 97.6% / `SysAiUsageService` 97.6% / `AiModelService` 94.5% / `AiImageService` 93.6% / `AiProviderService` 99.4% / `AiEmbeddingService` 86.7% / `AiChatToolService` 85% / `AiVideoService` 72.8% / `AiKnowledgeService` 66.4% / `AiChatService` 17.2%；另含 `FileService` 100% / `TtsRecordService` 95.6% / `SafeUrlValidator`(SSRF) / `ConchService` 94% / `RateLimitService`·`RateLimitInterceptor`·`AuthInterceptor` 100% / `WebUtils` 100% / `RequestLogInterceptor` 100% / `ExternalCallLogger` 90.2% / 6 个纯逻辑类 64–100%。
- **单元测试**：`src/test/java` 下为纯逻辑/Mockito 隔离单测（不启动 Spring、不连 DB/Redis），覆盖 `SafeUrlValidator`(SSRF 防护)、`SubjectUtils.getClientIp`、`AiKnowledgeService`(余弦检索排序 + 分块)、`AiEmbeddingService.toVectorLiteral`、`Result`/`BusinessException`，以及 AI 核心 service 全系：`AiModelService`/`AiImageService`/`AiMemoryService`/`SysAiUsageService`/`AiChatToolService`/`AiChatSessionService`/`AiVideoService`/`AiEmbeddingService`/`AiPersonaService`/`AiTemplateService`/`AiTaskService`/`AiTtsService`/`TtsService`/`AiChatService`(同步路径) 等（见各 `*Test.java`）。新增测试优先选纯逻辑/算法/工具类，用 Mockito 隔离协作者。约定：内联 `final HttpClient` 用 `com.stellar.test.ReflectUtil.setFinalField` 注入 mock；HTTP 发送用 `doReturn(resp).when(mock).send(any(), any())`（避免泛型 `<T>` 推断失败）；`InetAddress.getAllByName` 真实 DNS 用 `MockedStatic<InetAddress>`（预构建公网 IP 须在 mock 块外）绕过 `SafeUrlValidator`；登录态用 `MockedStatic<StpUtil>`；MyBatis-Plus `insert`/`updateById` 的 `(T)` 与 `(Collection<T>)` 重载歧义用 `ArgumentCaptor` 而非裸 `any()`。**Controller 测试**同样纯 Mockito：`new Controller(mockService)` 构造注入（`@RequiredArgsConstructor` 字段即构造参数），`@Mock` 隔离 Service，登录/游客分支用 `MockedStatic<StpUtil>`，IP 解析用 mock `HttpServletRequest` 打代理头（`X-Forwarded-For`/`X-Real-IP`/`getRemoteAddr`）验证主体落点；`AiNotifyController` 需 `RequestContextHolder.setRequestAttributes(new ServletRequestAttributes(mockReq))` 供游客分支取 request；`TtsController`/`FileController`/`ConchController` 用 `ResponseEntity` 断言 Content-Type/Cache-Control/Content-Disposition 头，`SysLogController.export` 用 `MockHttpServletResponse` 真实验证 xlsx 写出。

Verify before committing: `pnpm typecheck` (frontend) + `mvn -q compile -DskipTests` (backend).

## Gotchas that will bite you

**Auto-import dts are generated, not hand-written.** `src/types/auto-imports.d.ts` and `components.d.ts` are emitted by `unplugin-auto-import` / `unplugin-vue-components` during a vite run. If you add a new Vue/Pinia/vue-router API or a new Naive UI component, `pnpm typecheck` will fail with "Cannot find name X" until you run `pnpm dev` (or `pnpm exec vite build`) once to regenerate them. Don't edit those dts by hand.

**PowerShell mangles `-Dspring-boot.run.profiles=local`** (the dots split the arg). Use the `SPRING_PROFILES_ACTIVE=local` env var instead.

**The `local` profile is mandatory for real DB access.** `application.yml` uses `${DB_URL}` / `${DB_USERNAME}` / `${DB_PASSWORD}` placeholders with localhost defaults. The gitignored `application-local.yml` overrides them with the real remote PostgreSQL and also sets `spring.sql.init.mode=always` so `schema.sql` (idempotent `CREATE TABLE IF NOT EXISTS sys_user`) runs on startup. Without the local profile the app starts against localhost and will fail to connect.

**Redis also needs the `local` profile.** `application.yml` uses `${REDIS_URL:}` (empty default) + `database: ${REDIS_DATABASE:0}`. The gitignored `application-local.yml` sets `spring.data.redis.url` and `database: 1`. **url 末尾的 `/db` 在某些 Spring Boot 版本不被 Lettuce 解析（会落到 db0），必须显式 `spring.data.redis.database`/`REDIS_DATABASE` 指定 db**——部署时 `.env` 设 `REDIS_DATABASE=1`。Without the local profile Redis stays unconfigured — Sa-Token session persistence, IP rate-limiting, and `@Cacheable` all fail at runtime. Unlike the DB, an empty Redis URL won't crash startup (Lettuce connects lazily), so failures surface only on first use.

**Layout height uses viewport units, not `100%`.** `BasicLayout` is `position: absolute` + `height: 100vh`; the login page uses `min-height: 100dvh`. Don't switch these to `height: 100%` — the Naive UI provider chain (`NConfigProvider` → `NMessageProvider` → …) does not propagate height, so `100%` collapses to content height (this is why the login background used to stop at the card).

## Secrets

- `application-local.yml` is gitignored and holds the real DB + Redis credentials. **Never** copy its contents into `application.yml` or any tracked file. `application.yml` must keep only env-var placeholders.
- The DBX MCP connection credentials live in DBX storage (`%APPDATA%\com.dbx.app\dbx.db`), not in the repo. Add connections via the `dbx_add_connection` tool; don't put connection strings in `opencode.json`.

## Frontend conventions

- **Dev proxy**: Vite proxies `/api` → `http://localhost:8080`. Backend has no context path (serves at root). Frontend axios `baseURL` is `/api` (see `.env`).
- **Messages**: use `window.$message` / `window.$dialog` (discrete API, set up in `src/utils/discrete.ts`, follows the theme) for code outside components — notably the axios interceptor in `src/api/request.ts`. Use `useMessage()` inside components; it requires `NMessageProvider`, which is present in `App.vue`. **错误提示统一由 axios 拦截器处理**：HTTP 200 但 `code!=200` 按 `res.message` 弹 error（401 不弹、直接跳登录）；HTTP 错误优先用后端 `data.message`、超时弹"请求超时"、其余兜底"网络异常"；组件内 `catch` 不再重复 `message.error`（流式 fetch/纯前端操作除外）。
- **Menu is static and route-driven.** Routes live in `src/router/index.ts`; the sidebar is generated from the `Root` route's children by `src/composables/useMenu.ts`. To add a page, add a route (with `meta.title` / `meta.icon` / `meta.order`) — the menu updates automatically. **菜单按登录态 + 公开配置过滤**：`generateMenus(isLogin, publicKeys)` 未登录时渲染 `meta.requiresAuth === false`（天然公开，如 `/home`）或 `publicKeys` 命中的路由；`publicKeys` 由 `store/menu.ts` 从 `GET /public/menu-config` 拉取缓存，`LayoutMenu` 以 `authStore.isLogin`/`menuStore.publicKeys` 为响应式依赖，登录/退出后菜单自动刷新（`auth.login`/`logout` 会 `menuStore.reset()`）。路由守卫：未登录时先 `loadPublicConfig()`，对 `publicKeys` 命中的工具页放行游客访问，否则跳 `/login`（`vue-router` 的 `route.meta` 合并父→子，`Root` 默认 `requiresAuth: true`，子路由显式标 `false` 才天然公开）。游客态 `BasicLayout` 隐藏多标签页（`LayoutTabs v-if="authStore.isLogin"`），`LayoutHeader` 显示"登录"按钮。后台 `系统管理 → 游客访问配置`（`/system/menu-visibility`）勾选哪些菜单对游客公开。
- **Icons are string keys.** `meta.icon` is a string (e.g. `"grid"`) mapped to a `@vicons/ionicons5` component in `src/utils/icons.ts`. Add new icons to that map.
- **Mobile**: below 768px the persistent sidebar is replaced by a drawer (`src/composables/useBreakpoint.ts`). The header's menu button opens the drawer on mobile, toggles collapse on desktop.
- **落地页 `/home` + 关于我 `/about`**：均公开（`meta.requiresAuth: false`）。`/home` 精简门户，读 `/public/profile` + `menuStore.publicKeys` 渲染 hero + 公开工具卡片导航 + "关于我"入口；`/about` 简历式个人主页，读 `/public/profile` + `/public/profile-projects`，Hero + 富文本"关于我"(`v-html`) + 技能 + 项目展示卡片网格（名称/简介 + 访问线上/查看源码按钮）+ 联系方式。管理页 `system/profile` 用 `NUpload` custom-request + `/file/upload` 上传头像，额外编辑头衔 / 富文本 about（textarea + 实时预览）/ 所在地，底部内嵌"项目展示"`NDataTable` + `NModal` CRUD（项目名/线上地址/源码地址/简介）。`vite.config` 代理 `/file` → 8080。
- **游戏页 `/game/math`**：公开（`meta.requiresAuth: false`），一级菜单 `Game`（`icon: game`，order 6）+ 子菜单数学游戏（`icon: calculator`）。记忆型十以内加减法：30 题、每 3 秒闪现、第 6 题起滞后 5 题作答（题面隐藏）、计分/正确率/用时/逐题复盘；成绩提交 `POST /game/scores`、排行榜 `GET /game/scores/top`（`api/game.ts`）。登录用户默认填昵称。纯前端计时（`setInterval` 100ms 节拍驱动倒计时），无后端游戏逻辑。
- **神奇海螺页 `/game/conch`**：公开（`meta.requiresAuth: false`），Game 下子菜单（`icon: conch`，order 2）。海绵宝宝占卜道具玩法：输入框 + "拉绳"按钮 → `POST /game/conch/ask` 返回 `{answerId, answerText, audioUrl}` → 展示回答文本（大号文字淡入）+ `getConchAnswerAudio` 拉音频 blob 自动播放（用户点击属手势，浏览器允许自动播放）+ 海螺 🐚 抖动动画（`asking` 时 `shaking` class）。后台 `game/conch-admin`（`icon: conch`，order 3）用 `NTabs` 切"预设管理"（列表 + `NSwitch` 启停 + 试听 + `NModal` 增删改，`NUpload` custom-request 调 `uploadFile` 上传音频存 `sys_file` 拿 fileId）与"提问历史"两个 tab；AI 匹配开关改调 `GET/PUT /setting/conch_ai_enabled`（`SysSettingController`）。`api/conch.ts` 封装全部接口。
- **AI 配置页 `/system/ai-config`**：两级页面（`NDataTable` + `NModal`）。上半"AI 供应商"列表（名称/endpoint/apiKey 脱敏/启停/拉取模型/测试连通/编辑/删除，`fetchAiProviderModels` 落库 available_models），选中供应商后下半"模型管理"列表（模型名/类型标签/启停/设默认互斥/编辑/删除）。模型类型下拉写死 TEXT/IMAGE/AUDIO/EMBEDDING/VIDEO（与后端字典 `model_type` 一致）。`api/ai.ts` 封装 provider/model/image/setting 全部接口。
- **文案/图片/视频三页统一布局**：均用 `views/ai/components/AiGeneratorLayout.vue` 公共骨架——左 360px 配置栏（sticky）+ 右历史单卡 + 内置 `NDrawer` 壳，`<1024px` 栈叠单列（抽屉全屏）；三页填 `aside/history/drawer` slots。历史点"查看"开右侧抽屉（`v-model:drawer-open` + `#drawer` slot）回看，去掉 result 卡、生成中按钮 loading + `message` 提示，完成后自动弹抽屉展示最新一条，结果全走历史/抽屉，避免历史结果撑高把历史卡顶出视口。替换原 `max-width:720px` 单列居中以利用横向空间。
- **文案工具 `/ai/copy`**：登录态加载 TEXT 模型下拉（`getAiModelsByType('TEXT')`，默认选 isDefault），`streamAiChat` 第四参 opts 支持 `modelId`（选项目模型）/`endpoint+apiKey+model`（自带 key）/空（后端 TEXT 默认）。游客态仍可用自带 key 或后端默认模型。生成后右下常驻 `NDataTable` 历史（`getChatRecordPage`，后端流式结束自动落库 `sys_ai_chat_record`，登录按账号、游客按 IP），列=请求时间/提示词/结果/返回时间/耗时/操作，点击行或"查看"开右侧抽屉，用 `CopyResultDetail` 子组件（`parseCopyResult`）渲染该条 `result`；去掉 result 卡，生成中按钮 loading，完成后自动弹抽屉展示最新一条。游客也可见历史（按 IP）。不再调用 `sys_ai_copy_result`。
- **图片生成页 `/ai/image`**：公开（`meta.requiresAuth: false`），AI创作下子菜单（`icon: image`，order 3）。选 IMAGE 模型（`getAiModelsByType('IMAGE'`，接口 `@PublicAccess` 游客可查）+ prompt + size/ratio 下拉 → `POST /ai/image/create` 异步任务（`@RateLimit(daily=2)` 游客日限）返回 taskId → SSE 通知完成（`store/aiNotify` 长连接），completed 后自动弹抽屉用 `<img>` 展示大图+下载（url=`/file/{id}`，dev 代理）。生成后右下常驻 `NDataTable` 历史（`getAiImagePage` `@PublicAccess`，复用 `sys_ai_image_task`，按主体分页），列=请求时间/提示词/尺寸比例/结果(缩略图)/返回时间/耗时/操作，"查看"开右侧抽屉看大图+下载；去掉 result 卡，生成中按钮 loading，完成后自动弹抽屉展示最新一条。游客历史按 IP 可见。
- **视频生成页 `/ai/video`**：需登录，AI创作下子菜单（`icon: play`，order 4）。选 VIDEO 模型 + prompt + ratio/duration 下拉 → `POST /ai/video/create` 异步任务（`@RateLimit(daily=3)` 日限）返回 videoId → 后端 `AiVideoTaskWorker` @Async 主动轮询供应商（5s 间隔/10min 超时），SSE 通知完成，completed 后自动弹抽屉用 `<video>` 播放+下载（url=`/file/{id}`）。后端 `AiVideoService.createTask` 本地落库 `sys_ai_video_task`（存 ratio/duration 宽高帧率/videoId/subject），`getTask` 被 `AiVideoTaskWorker` 调用更新本地行 status/file_id（completed 复用本地 file_id 不重复下载）。生成后右下常驻 `NDataTable` 历史（`getAiVideoPage` 需登录，按账号分页），列=请求时间/提示词/比例时长/结果/返回时间/耗时/操作，"查看"开右侧抽屉播放+下载；去掉 result 卡，生成中按钮 loading，完成后自动弹抽屉展示最新一条。视频历史仅登录可见（生成本身需登录）。
- **文件管理页 `/system/file`**：系统管理下子菜单（`icon: folder`，order 9，需登录）。分页列表（`getFilePage`，后端联查 `sys_user.username` 取上传者名，不含二进制 data）+ 过滤（文件名模糊/类型 image·audio/时间范围）+ 详情抽屉（图片大图预览/音频 `<audio>` 播放 + 下载 + 元数据，不调 meta 接口直接用行数据，src=`/file/{id}`）+ 删除（单条 `NPopconfirm` 确认 + 多选批量删除，均硬删除）。列=复选/ID/预览缩略图/文件名/类型(ext tag)/大小/上传者/上传时间/操作。复刻 `system/log` 布局风格。
- **TTS 语音合成**（AI创作下 `/ai/tts`，Edge+AI 合一页 Tab 切换）：`api/tts.ts` 封装 `synthesizeEdgeTts`/`synthesizeAiTts`/`getTtsRecordPage`/`getTtsRecordAudio`/`deleteTtsRecord`（edge 60s、ai 120s blob 超时）；`constants/tts-voices.ts` 导出 Edge 发音人分组（`ttsVoiceOptions`）+ MiMo 预置音色分组（`mimoVoiceOptions`），`getVoiceLabel` 先查 Edge map 再查 MiMo map。页面用 `NTabs` segment 切 Edge/AI 两个 tab，共享音频预览区和最近合成列表。Edge tab 有 rate/pitch/volume 滑块；AI tab 有模型/音色/风格指令（需登录）。公开（`requiresAuth: false`），AI 合成本身需登录。

## Backend conventions

- **Auth = Sa-Token.** Token is sent as `Authorization: Bearer <token>`. Endpoints: `POST /auth/login`, `POST /auth/logout`, `GET /user/info`, `POST /user/change-password`. 鉴权由 `config/SaTokenConfig.java` 注册的 `AuthInterceptor`（`com.stellar.interceptor`）统一处理：**默认所有接口要求登录，仅标注 `@PublicAccess`（`common/annotation`）的 Controller 方法/类对游客放行**；未登录调用受保护接口抛 `NotLoginException`，由 `GlobalExceptionHandler` 转 401 envelope，前端 axios 拦截器据此重定向 `/login`。公开接口内可用 `StpUtil.isLogin()` 区分游客/登录做差异化处理。拦截器 `excludePathPatterns`：`/auth/login`、`/auth/register`、`/error`。文件读取走 `GET /file/{id}`（`@PublicAccess`），无需静态资源放行。公开接口集合 `PublicController`（`/public/**`，方法标 `@PublicAccess`），如 `GET /public/menu-config` 返回游客可见 route keys；`MenuVisibilityController`（`/menu-visibility/**`，需登录）管理可见性，存 `sys_menu_visibility` 表。
- **Redis 基础设施**: `spring-boot-starter-data-redis`（Lettuce + commons-pool2 连接池）+ `sa-token-redis-jackson`（Sa-Token 会话持久化到 Redis，重启不丢登录态）。配置 `application.yml` 用 `${REDIS_URL:}` + `${REDIS_DATABASE:0}` 占位符 + `spring.cache.type=redis`，真实地址密码写 gitignored 的 `application-local.yml`。**url 末尾 `/db` 在某些 Spring Boot 版本不被 Lettuce 解析（会落到 db0），必须显式 `spring.data.redis.database`（local 固定 1，部署用 `REDIS_DATABASE` 环境变量）指定 db**。`config/RedisConfig` 配 `RedisTemplate<String,Object>`（String key + JSON value）+ `RedisCacheManager`（cacheName 加 `stellar:` 前缀，默认 30min TTL，value 序列化器用 `GenericJackson2JsonRedisSerializer.builder().objectMapper(new ObjectMapper().registerModule(new JavaTimeModule())).defaultTyping(true).build()`——注册 JavaTimeModule 支持 `LocalDateTime`，并显式开启 default typing 写入 `@class` 类型标记，避免缓存的 POJO 反序列化成 `LinkedHashMap` 导致 `ClassCastException`；注：builder 传自定义 ObjectMapper 时 typing 默认关闭，必须 `.defaultTyping(true)` 显式开启）。**所有业务缓存 key 统一以 `stellar:` 开头**与其他项目隔离：Spring Cache 经 `prefixCacheNameWith` 自动加前缀，直接用 RedisTemplate 的场景经 `common/CacheConstants` 常量拼接（如限流 `stellar:rate-limit:{ip}:{date}`）。Sa-Token 会话 key 保持默认 `Authorization:` 前缀（会话数据非业务缓存，已天然隔离）。**被 `@Cacheable` 标注的方法返回 `List` 时必须用 `.collect(Collectors.toList())`（返回 `ArrayList`），不可用 `Stream.toList()`（返回 `ImmutableCollections$ListN`，Jackson 反序列化失败）**。Spring Cache 落点：`SysDictService.listDataByCode` 标 `@Cacheable(cacheNames="dict", key="#dictCode")`（key=`stellar:dict::{dictCode}`，字典无写入接口故无需 `@CacheEvict`，将来加字典管理接口需补）。新增缓存落点（均默认 30min TTL，写操作 `@CacheEvict` 失效）：`menu-visibility`（`MenuVisibilityService.listPublicRouteKeys`/`listAll` 读，`batchUpsert` `allEntries` 清）；`profile`（`ProfileService.get` 读，`update` `allEntries` 清）；`profile-project`（`ProfileProjectService.list` 读，CRUD `allEntries` 清）；`ai-model`（`AiModelService.listEnabledByType`/`listAll`/`listByProvider` 读，模型 CRUD `allEntries` 清）；`ai-provider`（`AiProviderService.list` 读，供应商 CRUD `allEntries` 清，`delete`/`update`/`toggleEnabled`/`fetchModels` 用 `@Caching` 同时清 `ai-model`，因供应商启停/删除/拉模型会影响按类型查模型结果）；`setting`+`setting-bool`（`SysSettingService.get` 缓存 String、`getAsBoolean` 缓存 Boolean，分两个 cacheName 避免同 key 类型冲突，`set` 用 `@Caching` 精确清两份——`getAsBoolean` 不调 `get` 是为绕开 Spring AOP 自调用不走代理的限制，改调 private `doGet` 复用查库逻辑）。
- **Passwords are BCrypt.** `PasswordConfig` exposes a `PasswordEncoder` bean; `DataInitializer` seeds `admin / 123456` on first startup if missing. Don't store plaintext passwords.
- **Response envelope**: all controllers return `Result<T>` (`{ code, message, data }`); `code === 200` means success. `GlobalExceptionHandler` 统一捕获 `BusinessException`/校验异常/`NotLoginException`/`Exception`，所有异常日志带请求上下文（method/URI），系统异常带完整堆栈，校验异常记录失败字段；traceId 由 `RequestLogInterceptor` 放入 MDC 自动输出，与请求日志关联排查。
- **MyBatis-Plus**: `sys_user` uses logic delete on the `deleted` column (0/1). The PG pagination dialect is configured in `MyBatisPlusConfig`.
- **操作日志 = AOP + 注解**: Controller 方法标注 `@Log(title="模块", type=OperationType.X)`，由 `LogAspect`（`@Around`）统一采集模块/类型/操作人/请求方法+URL/Java方法/参数(脱敏)/状态/异常/IP/耗时，异步（`@Async("logTaskExecutor")`）写入 `sys_log` 表。**QUERY 类成功不落库（降噪），但 QUERY 报错（NPE/数据异常等）仍记录**，便于第一时间发现 bug；增删改/登录登出/外部调用正常全量记录。操作人解析：LOGIN 取请求体 username，其余取 `StpUtil` 登录态。脱敏硬编码 `password/oldPassword/confirmPassword/token/secretKey/apiKey` → `******`（按参数名组装成 JSON 对象后正则命中，`@RequestParam` 与 `@RequestBody` 均覆盖）。日志查询/详情/导出(xlsx, EasyExcel) 在 `SysLogController`，前端页在 `系统管理 → 日志管理`。新增可记录的操作时，给对应 Controller 方法加 `@Log` 即可。
- **外部接口调用日志 = ExternalCallLogger**: `ExternalCallLogger`（`com.stellar.infra`）统一记录第三方接口调用：运行日志 `[外部调用] 成功/失败 provider=... action=... durationMs=...` + 复用 `sys_log` 异步落库（module=`外部调用`、operationType=`OTHER`、requestUrl=`provider / action`、params=脱敏调用参数、status=1/0、errorMsg、duration）。异步线程无 web 上下文时传同步阶段捕获的 `operator`（subject=`account:{userId}`/`ip:{ip}`）避免降级。已接入：LLM 流式/多轮流式/工具判定/工具后流式/非流式（`AiChatService`）、AI 图片生成（`AiImageTaskWorker.generateImageBytes`）、AI 视频创建/查询（`AiVideoService`）、AI TTS（`AiTtsService`）、Edge TTS（`TtsService`）、Embedding（`AiEmbeddingService`）。后台 `系统管理 → 日志管理` 按模块 `外部调用` 可查。
- **运行日志 = logback-spring.xml**:
 `backend/src/main/resources/logback-spring.xml` 控制台 + 文件双输出。当前日志 `backend/logs/stellar.log`，归档 `backend/logs/archived/stellar-yyyy-MM-dd.N.log`，`SizeAndTimeBasedRollingPolicy` 按天切 + 单文件 50MB 上限，保留 30 天，总量上限 2GB。级别：`com.stellar` 包 DEBUG，其余 INFO（`application.yml` 的 `logging.level` 仍生效，与 xml 一致）。`*.log` 已在 `.gitignore`，日志文件不会误提交。日志格式含 `[%X{traceId:-}]`，traceId 由 `RequestLogInterceptor`（`com.stellar.interceptor`，注册在 `SaTokenConfig` 最先执行）为每个请求生成 8 位短码放入 MDC，贯穿该请求全链路日志；`afterCompletion` 记录 `请求完成 {method} {uri} status={status} 耗时={ms}`（status>=400 用 warn）。异常日志经 `GlobalExceptionHandler` 统一带请求上下文（method/URI），通过 traceId 与请求日志关联排查。
- **AI 模块**: `com.stellar.ai` 含内置模板种子数据。LLM 配置改为多供应商多模型：`sys_ai_provider`（name/endpoint/apiKey/available_models 缓存/enabled）+ `sys_ai_model`（provider_id/model/model_type/is_default/enabled，按类型设默认互斥）两表，替代旧单条 `sys_ai_config`（启动时 schema.sql DO 块幂等迁移旧配置→1 供应商+1 TEXT 模型，旧表保留不删）。模型类型用字典表 `sys_dict_type`+`sys_dict_data` 管理（`SysDictService.listDataByCode`，首批种 model_type：TEXT/IMAGE/AUDIO/EMBEDDING/VIDEO，后三种预留）。全局开关迁 `sys_setting`（key-value，`SysSettingService` get/set/getAsBoolean，`SysSettingController` GET/PUT `/setting/{key}`，`conch_ai_enabled` 迁入）。`AiProviderService`（CRUD + `fetchModels` 落库 available_models + `testConnection`）+ `AiModelService`（CRUD + `setDefault` 同类型互斥 + `resolveConfig(modelId)`/`resolveDefaultConfig(type)` 返回 `AiResolvedConfig` record：endpoint/apiKey/model/modelType/providerId）。apiKey 脱敏返回前端，更新时为空保留原值。流式对话通过 `SseEmitter` + JDK `HttpClient` 代理转发 LLM 的 SSE 流，前端用 `fetch`+`ReadableStream` 读取（可带 Authorization header，绕过 `EventSource` 限制）；错误统一发默认 message 事件 `{"error":...}`（不用命名事件，前端只解析默认 data），聊天页据此渲染失败原因。`AiChatService` 按 modelId 解析配置（自带 key>modelId>TEXT 默认），`streamChat(ChatRequest)`/`streamChat(modelId,prompt)`/`chatCompletion(prompt)`（TEXT 默认）/`chatCompletion(modelId,prompt)`。`doStreamChat` 流式结束自动落库 `sys_ai_chat_record`（请求时间/返回时间/提示词/结果，登录按账号、游客按 IP，带幂等守卫异常吞掉）。模板（`sys_ai_template`）存于 DB；旧文案历史表 `sys_ai_copy_result` 及 `AiCopyResultController`/`Service`/实体/Mapper/DTO 已被 `sys_ai_chat_record` 取代并删除（DB 表数据保留）。`AiDataInitializer` 启动时播种 3 套内置模板（B站/抖音/小红书，`built_in=1` 不可删，可"恢复默认"）。前端页面：系统管理 → AI 配置（两级）/ AI 模板管理；视频工具箱 → 文案工具 / AI 图片生成。封面工具（画布编辑、草稿）仍为纯前端 LocalStorage。
- **AI 外部 URL 安全**: 用户自带 AI endpoint 经 `SafeUrlValidator` 限定为公开 `http/https` 地址，拒绝 localhost、本机/私网及解析到特殊用途地址的域名；图片/视频供应商产物 URL 复用同一校验，`HttpClient` 禁止重定向，并以流式限长读取（图片 20MB、视频 200MB）后才写 BYTEA。JDK `HttpClient` 无 DNS 绑定接口，校验覆盖请求前全部 DNS 解析结果并保持不跟随重定向。
- **AI 计费/统计**: 每次 LLM 调用记录 token 消费到 `sys_ai_usage`（subject_type=account/ip，subject_id=userId/IP，provider_id/model_type 维度，prompt/completion/total_tokens，source=usage/estimate）。`AiChatService` 请求加 `stream_options.include_usage`，LLM 返回则记精确值，否则字符估算兜底（source=estimate）。`AiChatController#/stream` `@PublicAccess + @RateLimit(daily=5)` 对游客开放（IP 日限 5 次）；`GET /dashboard/stats`（`DashboardController`，需登录）聚合仪表盘全部统计（总/今日 token、调用次数、近 7 日趋势、按类型/供应商分组，+文案/图片/视频任务的成功率与平均耗时，+文件总数/今日上传/总占用/按类型分布，+TTS 总数/今日合成/总音频大小），首页 `dashboard` 展示。文案工具 `/video/copy` 游客态：`/ai/template/page` `@PublicAccess` 加载模板，生成后下方常驻 `NDataTable` 历史（`getChatRecordPage` `@PublicAccess`，按 IP 查询，后端流式结束自动落库 `sys_ai_chat_record`），点击行开右侧抽屉用 `CopyResultDetail`（`parseCopyResult`）渲染该条 `result`；游客历史按 IP 可见（同 IP 共享，已知隐私折衷）。用户可自带 AI 配置：文案工具"自己的 AI"按钮（复用 `ApiSettingsModal`，存 localStorage `apiConfigStore`），`streamAiChat` 调用时传 endpoint/apiKey/model 给 `/ai/chat/stream`，`AiChatService.streamChat(ChatRequest)` 优先用传入配置（后端不持久化 key），无则按 modelId 解析，再无则回退 TEXT 默认模型。自带 key 仍受 `@RateLimit` 限制。图片生成 `AiImageService` 调 `/v1/images/generations`（response_format=b64_json），b64 解码存 `sys_file` 永久化，`POST /ai/image/create` 异步任务 `@PublicAccess+@RateLimit(daily=2)` 游客开放（IP 日限 2 次，返回 taskId，`@Async` 生成+存库，前端 SSE 通知完成），`GET /ai/image/page` `@PublicAccess` 历史分页（复用 `sys_ai_image_task`，按主体），图片 API 无 usage 按 prompt 字符估算记 token。
- **AI 任务通知 = SSE + Redis pub/sub**: 图片/视频生成完成不再前端轮询，改用 SSE 长连接推送。`SseEmitterManager`（`com.stellar.infra`）按 subject（`account:{userId}`/`ip:{ip}`）分组管理 `SseEmitter`，`@Scheduled(fixedRate=30000)` 心跳保活 + 检测死连接。`AiNotifyController`（`/ai/notify`，`@PublicAccess`）返回 `SseEmitter`（24h 兜底超时），登录按 account、游客按 IP 订阅。任务完成时 `AiNotifyPublisher.publish` → `redisTemplate.convertAndSend("stellar:ai:notify", AiNotifyMessage)` 广播，各实例 `AiNotifyListener`（`MessageListener`，`GenericJackson2JsonRedisSerializer` 反序列化）订阅后调 `SseEmitterManager.send` 推本地连接。图片 `AiImageTaskWorker.doGenerateAsync` 完成/失败时发通知；视频 `AiVideoTaskWorker`（`@Async("aiTaskExecutor")` + `@EventListener(VideoTaskCreatedEvent)`）循环轮询供应商（5s 间隔/10min 超时，复用 `AiVideoService.getTask`），完成/失败/超时发通知；`AiVideoService.createTask` 落库后 `eventPublisher.publishEvent` 触发 worker。`RedisConfig` 注册 `RedisMessageListenerContainer`，`AsyncConfig` 加 `@EnableScheduling`。前端 `store/aiNotify.ts` 用 `fetch`+`ReadableStream` 连 `/ai/notify`（带 Authorization header），断线指数退避重连，`onTaskNotify` 注册回调；`BasicLayout` 全局建立/断开/登录态变化重连。
- **文件上传/管理**: `FileController` `POST /file/upload`（需登录，白名单=图片+音频，集中在 `common/FileConstants`）将二进制存数据库 `sys_file` 表（BYTEA，`@TableField(select=false)` 列表不加载，按需 `FileService.getFull`→`selectFullById` 查询），上传时记 `user_id=StpUtil.getLoginIdAsLong()`（历史数据/系统生成如 AI 产物留 NULL），返回 `/file/{id}`；`GET /file/{id}` 标 `@PublicAccess` 游客可读（头像等公开图片），命中即跳过登录校验，响应可长期缓存。无磁盘卷依赖。`spring.servlet.multipart` 限 10MB。管理接口均需登录：`GET /file/page`（`SysFileQueryDTO`：originalName/fileType(image·audio)/userId/时间，`FileService.page` 联查 `sys_user.username` 取 `uploaderName`，返回 `SysFileVO` 不含 data）、`DELETE /file/{id}`、`DELETE /file/batch`（body=ids 数组，硬删除，引用方头像/海螺音频/AI 产物悬空自负）。个人介绍 `sys_profile`（单条 id=1，字段 nickname/avatar/bio/skills/links/title/about/location，`ProfileController` 管理 + `/public/profile` 游客读取）；个人项目展示 `sys_profile_project`（多条，字段 name/site_url/source_url/description，`ProfileProjectController` `/profile/project/**` 需登录 CRUD + `/public/profile-projects` 游客读取，about 页卡片网格公开展示）。
- **IP 单日限流**: `@RateLimit(daily=N)`（`common/annotation`）标在耗资源接口，由 `RateLimitInterceptor`（`com.stellar.interceptor`，注册在 `AuthInterceptor` 之后）按 IP+当日 计数，超限抛 `BusinessException(TOO_MANY_REQUESTS=429)`，经 `GlobalExceptionHandler` 转 429 envelope。**登录用户跳过限流（仅对游客计数）**。`RateLimitService` 基于 Redis INCR + 当日过期（key=`stellar:rate-limit:{ip}:{date}`，多实例共享计数）。阈值 `rate-limit.default-daily`（yml，默认 50）。对游客开放的耗资源接口（如 `TtsController#/edge/synthesize` `@PublicAccess + @RateLimit(daily=20)`）才限流；纯展示类公开接口（关于我/菜单配置）无限流。AI 文案 `/ai/chat/stream` 阶段4 已开放（`@PublicAccess+@RateLimit(daily=5)`，token 统计见下）。TTS 合成历史 `/tts/record/page` + `/tts/record/{id}/audio` `@PublicAccess` 作公共墙只读（游客可看全部合成、试听下载，删除仍需登录）。
- **游戏模块**: `GameController`（`/game/**`，`@PublicAccess`）对游客开放数学游戏成绩提交与排行榜。`sys_game_score` 表存 player_name/score/total_time(秒)/accuracy(%)/user_id(可空)/ip/create_time。`POST /game/scores` `@PublicAccess + @RateLimit(daily=30)` 提交成绩（`GameScoreSubmitDTO` 校验分数 0-30/正确率 0-100，游客填姓名、登录记 userId 兜底昵称）；`GET /game/scores/top` `@PublicAccess` 返回前 100（分数降序→用时升序→时间升序）。`GameScoreService` 用 `StpUtil.isLogin()` 区分游客/登录。IP 获取逻辑与 `RateLimitInterceptor` 一致（穿透 X-Forwarded-For/X-Real-IP）。
- **神奇海螺模块**: `ConchController`（`/game/conch/**`）对游客开放提问与音频读取。`conch_answer` 表存 answer_text/match_description/file_id(引用 sys_file)/enabled/sort_order；`conch_record` 表存 question_text/answer_id/user_id(可空)/create_time（**不记 IP**）。`POST /game/conch/ask` `@PublicAccess + @RateLimit(daily=10)` 提问：`ConchService.ask` 取启用预设 → `AiChatService.chatCompletion`（非流式同步方法，用 TEXT 默认模型，记录 token）让 LLM 返回 `{"ids":[top1,top2,top3]}` → 随机选 1，失败/格式错全库随机兜底 → 存 `conch_record` → 返回 `{answerId, answerText, audioUrl}`。AI 匹配开关读 `sys_setting`（key=`conch_ai_enabled`，`SysSettingService.getAsBoolean`，关闭则纯随机不调 LLM）。`GET /game/conch/answer/{id}/audio` `@PublicAccess` 取音频（按 `conch_answer.file_id` 查 `sys_file.data`，可长期缓存）。预设管理 `POST/PUT/DELETE /game/conch/answer`、`PUT /game/conch/answer/{id}/enabled`、`GET /game/conch/answer/page` 需登录；`GET /game/conch/record/page` 提问历史分页（关联查 answerText）。`FileController` 白名单已扩展支持音频（mp3/wav/m4a/aac/ogg）。
- **统一 AI 任务历史 = ai_task 表**: 合并 `sys_ai_chat_record`/`sys_ai_image_task`/`sys_ai_video_task`/`tts_record` 四张表的查询视图为统一 `ai_task` 表（task_type=text/image/video/tts 鉴别器，subject_type/subject_id 主体，prompt/result/status/file_id/file_data/extra JSONB 类型专属参数）。旧表保留不删，schema.sql 启动时幂等迁移旧数据。`AiTaskController`（`/ai/task`，`@PublicAccess`）统一分页/删除/清空 API。前端 `AiTaskHistory.vue`（`views/ai/components/`）通用历史组件，按 taskType 复用。`SubjectUtils`（`com.stellar.infra`）统一主体标识：`subjectType()`/`subjectId(request)`/`subjectKey(request)`/`getClientIp(request)`，消除各处重复的 IP 解析逻辑。
- **TTS 语音合成模块**（Edge + AI 双引擎，共用 `tts_record` 表）: `TtsController`（`/tts`）挂两条合成路径 + 公共墙历史。`tts_record` 表存 text/voice/rate/pitch/volume/audio_data(BYTEA)/file_size/audio_format(mp3·wav)/operator/create_time，`audio_data` 标 `@TableField(select=false)` 列表不加载，`audio_format` 区分来源（Edge=mp3，AI=wav），试听/下载时按 format 设 Content-Type（`TtsRecordService.getAudio` 返回含 audioFormat 的 `TtsRecord`）。
  - **Edge TTS**: `TtsService.synthesize` 直连 Microsoft Edge TTS WebSocket（`wss://speech.platform.bing.com`，硬编码 TrustedClientToken + SHA-256 Sec-MS-GEC 签名，`EdgeTtsListener` 收 `Path:audio` 二进制帧拼 MP3），文本>4096B 按句末标点切段逐段合成拼接。`POST /tts/edge/synthesize` `@PublicAccess + @RateLimit(daily=20)` 对游客开放，返回 MP3，存记录 audio_format=mp3。
  - **AI TTS**: `AiTtsService.synthesize(modelId, text, voice, style)` 走 OpenAI 兼容 chat completions + audio 参数（对接 MiMo-V2.5-TTS 预置音色模式）：按 `AiModelService.resolveConfig(modelId)` 解析 AUDIO 类型模型配置（endpoint/apiKey/model，`sys_ai_provider`+`sys_ai_model` model_type=AUDIO），请求 `/v1/chat/completions`，messages 为 `[{role:user, content:风格指令(可空)}, {role:assistant, content:合成文本}]`，`audio={format:wav, voice:预置音色}`，非流式响应 `choices[0].message.audio.data` 为 base64 WAV，解码返回 byte[]。每次记录 token 消费到 `sys_ai_usage`（model_type=AUDIO，LLM 返回 usage 用精确值否则字符估算）。`POST /tts/ai/synthesize` **需登录**（不加 `@PublicAccess`，无限流），返回 WAV，存记录 audio_format=wav（rate/pitch/volume 记默认 1.0，风格靠 user 消息自然语言/标签控制不单独存字段）。
  - **公共墙历史**: `GET /tts/record/page` `@PublicAccess` 分页（不加载 audio_data），`GET /tts/record/{id}/audio` `@PublicAccess` 取音频（按 audio_format 设 `audio/mpeg`·`audio/wav` + inline filename 后缀），`DELETE /tts/record/{id}` 需登录逻辑删除。Edge 与 AI 合成历史合并展示，前端按 audio_format 区分来源标签。`DashboardService` 聚合 `tts_record` 总数/今日/总字节统计。

## DBX MCP

`opencode.json` registers a `dbx` MCP server (the `@dbx-app/mcp-server` global npm bin). Config is loaded once at opencode startup — **after editing `opencode.json`, restart opencode** for changes to take effect. Use the `dbx_*` tools to inspect schemas and run SQL against the configured PostgreSQL.
