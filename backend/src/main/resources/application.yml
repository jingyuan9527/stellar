server:
  port: 8080
  servlet:
    context-path: /
  # 个人站并发低，收敛 Tomcat 工作线程（默认 200）省线程栈内存（每线程 ~1MB）
  tomcat:
    threads:
      max: 40
      min-spare: 4
    accept-count: 100

spring:
  application:
    name: stellar-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL:jdbc:postgresql://localhost:5432/soybean}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      # 个人站并发低，收敛连接池省内存/连接（每连接 ~1MB 堆外）
      maximum-pool-size: 8
      minimum-idle: 2
      connection-timeout: 3000
      max-lifetime: 1800000
  data:
    redis:
      url: ${REDIS_URL:}
      # 显式 db：url 末尾 /db 在某些 Spring Boot 版本不被 Lettuce 解析（会落到 db0），必须显式指定
      database: ${REDIS_DATABASE:0}
      lettuce:
        pool:
          # 个人站并发低，收敛连接数省内存；max-wait 有界避免无限阻塞
          max-active: 4
          max-idle: 4
          min-idle: 0
          max-wait: 1000ms
  cache:
    type: redis
    redis:
      time-to-live: 30m
  sql:
    init:
      mode: never
  jackson:
    default-property-inclusion: non_null
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    # SQL 日志开关：默认 NoLoggingImpl 关闭（Preparing/Parameters 噪声大）
    # 需要调试 SQL 时设环境变量 MYBATIS_LOG_IMPL=org.apache.ibatis.logging.slf4j.Slf4jImpl（走 logback 级别控制）
    log-impl: ${MYBATIS_LOG_IMPL:org.apache.ibatis.logging.nologging.NoLoggingImpl}
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

sa-token:
  token-name: Authorization
  # 登录态绝对有效期 7 天（原 30 天），期间不因活跃续期（active-timeout=-1），避免 Redis 会话长期堆积
  timeout: 604800
  active-timeout: -1
  is-share: false
  token-style: uuid
  is-log: false
  token-prefix: Bearer

# Actuator：仅暴露 health（公开监控）。只返回最基础 status，不泄露组件细节（db/redis/jvm 详情后边迭代再开放）
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

rate-limit:
  default-daily: ${RATE_LIMIT_DAILY:50}

# RAG 检索管线配置（登录用户聊天）：改写/重排/判定各一次 LLM 调用，无需可关
stellar:
  rag:
    top-k: 4
    pool-size: 12
    score-threshold: 0.0
    rewrite-enabled: true
    rerank-enabled: true
    memos-enabled: true
    loop-enabled: true
    loop-max-rounds: 3
    router-enabled: true
    router-simple-length: 12
    # 混合检索：BM25 关键词通道与稠密向量经 RRF 融合（专有名词/编号/标签精确召回）
    hybrid-enabled: true
    # 单次重排候选上限（应与 pool-size 联动，调大 pool-size 时同步调大）
    rerank-max-candidates: 12
    # 相关性闸门：短查询且无触发词判闲聊，跳过整个 RAG 管线（省 embedding + LLM 调用）
    retrieval-gate-enabled: true
    retrieval-trigger-words: 搜索,查找,查,笔记,备忘,知识库,资料,文档,记录,关于,是什么,怎么,如何,多少,价格,介绍,记得,总结,回顾,内容,找
  # 登录爆破防护（AuthService）
  security:
    login-max-fail: 5
    login-lock-minutes: 15
    login-ip-max-attempts: 10
    login-ip-window-minutes: 10

logging:
  level:
    com.stellar: ${APP_LOG_LEVEL:debug}
